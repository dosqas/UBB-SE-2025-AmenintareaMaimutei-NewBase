<Page
    x:Class="CourseApp.Views.CoursePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:CourseApp.ViewModels">
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Text="{Binding CurrentCourse.Title}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding CurrentCourse.Description}" TextWrapping="Wrap" Margin="0,0,0,20"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="Back" Click="BackButton_Click" Margin="0,0,10,0"/>
                <Button Content="Enroll" Command="{Binding EnrollCommand}"
                        IsEnabled="{Binding IsEnrolled, Converter={StaticResource InverseBooleanConverter}}"/>
            </StackPanel>
            <TextBlock Text="Modules:" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>
            <ListView ItemsSource="{Binding Modules}" x:Name="ModulesListView" IsItemClickEnabled="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Gray" BorderThickness="1" Padding="5" Margin="5">
                            <StackPanel Orientation="Vertical">
                                <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Description}" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
